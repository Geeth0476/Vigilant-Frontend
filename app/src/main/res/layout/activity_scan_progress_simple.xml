<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/white"
    tools:context=".ScanProgressActivity">

    <!-- Center Content Container -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:gravity="center_horizontal"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:padding="32dp">

        <!-- Icon Circle -->
        <FrameLayout
            android:layout_width="120dp"
            android:layout_height="120dp"
            android:layout_marginBottom="32dp">
            
            <!-- Outer Circle / Pulse Effect (Static for now) -->
            <View
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:background="@drawable/bg_circle_primary"
                android:backgroundTint="#E8EAF6"/> <!-- Very Light Blue -->

            <!-- Inner Blue Circle -->
            <View
                android:layout_width="80dp"
                android:layout_height="80dp"
                android:background="@drawable/bg_circle_primary"
                android:backgroundTint="@color/vigilant_blue"
                android:layout_gravity="center"/>

            <!-- Icon -->
            <ImageView
                android:layout_width="40dp"
                android:layout_height="40dp"
                android:src="@drawable/ic_shield_scan_white" 
                android:layout_gravity="center"/>
        </FrameLayout>

        <!-- Title -->
        <TextView
            android:id="@+id/tvSimpleTitle"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Scanning Device..."
            android:textSize="24sp"
            android:textStyle="bold"
            android:textColor="@color/black"
            android:layout_marginBottom="8dp"/>

        <!-- Subtitle -->
        <TextView
            android:id="@+id/tvSimpleDesc"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Checking for spyware and threats"
            android:textSize="16sp"
            android:textColor="@color/vigilant_text_secondary"
            android:layout_marginBottom="32dp"/>

        <!-- Linear Progress -->
        <com.google.android.material.progressindicator.LinearProgressIndicator
            android:id="@+id/progressSimple"
            android:layout_width="200dp"
            android:layout_height="wrap_content"
            android:indeterminate="false"
            android:max="100"
            android:progress="0"
            app:trackColor="#E0E0E0"
            app:indicatorColor="@color/vigilant_blue"
            app:trackCornerRadius="4dp"
            app:trackThickness="6dp"/>
            
    </LinearLayout>

</androidx.constraintlayout.widget.ConstraintLayout>

